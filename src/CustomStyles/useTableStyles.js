// Define custom styles
import { makeStyles } from "@material-ui/core/styles";

const useTableStyles = makeStyles((theme) => ({
  tableHead: {
    background: "#f2f2f2", // Add your desired background color here
  },
}));
export default useTableStyles;
